spring.application.name=sgl-backend

# Desenvolvimento normal usa PostgreSQL.
# O profile demo (H2) deve ser ativado explicitamente quando necessario.
spring.profiles.active=dev

# SERVIDOR
server.port=8080

# JPA
spring.jpa.show-sql=true
